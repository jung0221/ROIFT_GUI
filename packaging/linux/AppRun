#!/bin/sh
# AppImage entry point: choose the C++ runtime, then start the application.
HERE="$(dirname "$(readlink -f "$0")")"
ROIFT_CXXRT_DIR="$HERE/usr/lib/cxxrt"
. "$ROIFT_CXXRT_DIR/select.sh"
exec "$HERE/usr/bin/roift_gui" "$@"
